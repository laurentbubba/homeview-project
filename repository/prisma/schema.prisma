generator client {
  provider = "prisma-client-js"
  binaryTargets   = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // TODO: add url for dev and prod
}

model Task {
  id            Int        @id @default(autoincrement())
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  name          String
  description   String
  isFinished    Boolean
}

// model Lecturer {
//   id        Int        @id @default(autoincrement())
//   createdAt DateTime   @default(now())
//   updatedAt DateTime   @updatedAt
//   expertise String
//   userId    Int        @unique
//   user      User       @relation(fields: [userId], references: [id])
//   courses   Course[]
//   schedules Schedule[]
// }

// model Course {
//   id          Int        @id @default(autoincrement())
//   createdAt   DateTime   @default(now())
//   updatedAt   DateTime   @updatedAt
//   name        String
//   description String
//   phase       Int
//   credits     Int
//   schedules   Schedule[]
//   Lecturers   Lecturer[]
// }

// model Schedule {
//   id         Int       @id @default(autoincrement())
//   createdAt  DateTime  @default(now())
//   updatedAt  DateTime  @updatedAt
//   start      DateTime
//   end        DateTime
//   course     Course    @relation(fields: [courseId], references: [id])
//   courseId   Int
//   lecturer   Lecturer  @relation(fields: [lecturerId], references: [id])
//   lecturerId Int
//   students   Student[]
// }

// model User {
//   id        Int       @id @default(autoincrement())
//   username  String    @unique
//   firstName String
//   lastName  String
//   email     String    @unique
//   password  String
//   role      String
//   lecturer  Lecturer?
//   student   Student?
// }